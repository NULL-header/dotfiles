# After soon, it will updated.
# It is so troublesome at search;
# Because it needs some jumping to anchor;
# Before search.
abbr --add ggr 'w3m google.com'
abbr --add g 'git'
# trans English to Japanese
abbr --add ja 'trans en:ja -pager /usr/bin/batcat'
# trans with reverse of the line above
abbr --add en 'trans ja:en -pager /usr/bin/batcat'
abbr --add tt 'taskwarrior-tui'
# open vim at root directory of git
# With old git, it will not work.
# this is shortened of "open vim at root directory"
abbr --add rv 'nvim (git rev-parse --show-toplevel)'
abbr --add clip 'win32yank.exe -i'
abbr --add paste 'win32yank.exe -o'
